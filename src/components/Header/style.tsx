import styled from 'styled-components';
import { AppBar } from '@mui/material';

export const Container = styled(AppBar)`
  background-color: #3f51b5; // Ajuste a cor do seu Header aqui
  padding: 10px 0;

  .MuiToolbar-root {
    justify-content: center;
  }
`;
